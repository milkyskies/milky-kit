[project]
name = "ts-firebase"
worktree_dir = "ts-firebase-worktrees"

[[apps]]
name = "client"
template = "react"
variants = { ui = "base-ui", auth = "firebase" }

[[apps]]
name = "api"
template = "hono"
variants = { db = "supabase", api_style = "rpc", auth = "firebase" }

[skills]
include = [
    "ship",
    "rulify",
    "retrospective",
    "land",
]
