[project]
name = "ts-supabase"
worktree_dir = "ts-supabase-worktrees"

[[apps]]
name = "client"
template = "react"
variants = { mobile = "capacitor" }

[[apps]]
name = "api"
template = "hono"
variants = { db = "supabase", api_style = "rpc" }

[skills]
include = [
    "ship",
    "rulify",
    "retrospective",
    "land",
]
