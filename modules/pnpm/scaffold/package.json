{
	"name": "{{project_name}}",
	"private": true,
	"packageManager": "pnpm@10.29.3",
	"engines": {
		"node": ">=22"
	},
	"pnpm": {
		"onlyBuiltDependencies": [
			"@biomejs/biome",
			"@tailwindcss/oxide",
			"esbuild",
			"sharp",
			"workerd"
		],
		"//trustPolicy": "Refuse install if a package's trust level decreased from a prior version (e.g. previously had provenance, new version doesn't). Catches the common compromise pattern where an attacker republishes from a stolen account without the original publisher's signing setup. (pnpm >= 10.21)",
		"trustPolicy": "no-downgrade",
		"//blockExoticSubdeps": "Refuse transitive deps from non-registry sources (git URLs, tarballs, local paths). Direct deps can still use exotic sources when explicitly declared, but a transitive dep is never allowed to drag in code from outside the registry. (pnpm >= 10.26)",
		"blockExoticSubdeps": true,
		"//overrides": "Pins transitive dep versions across the whole graph. Use when a vulnerability is identified in a transitive dep that direct deps haven't patched yet — see .claude/rules/security.md. Document each pin in a sibling `//overrides-<name>` key (NOT inside `overrides` — pnpm rejects `//`-prefixed package selectors).",
		"//overrides-esbuild": "GHSA-67mh-4wv8-2f99 — esbuild < 0.25.0 lets any website send requests to dev server. Some older deps (vite plugins, drizzle-kit, etc.) still pull older esbuild as transitive.",
		"overrides": {
			"esbuild": "^0.25.0"
		}
	}
}
