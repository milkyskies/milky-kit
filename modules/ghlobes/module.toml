# The .ghlobes.toml.template is a hint; actual config is generated by
# `glb init` in the project. Scaffold ships the template + a note.

[[files]]
src = "../scaffold/.ghlobes.toml.template"
dest = ".ghlobes.toml.template"
