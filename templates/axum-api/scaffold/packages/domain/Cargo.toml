[package]
name = "domain"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "2"
async-trait = "0.1"
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
utoipa = "5"
